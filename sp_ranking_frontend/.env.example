# Environment configuration for S&P Ranking Frontend
# IMPORTANT: Do not commit real secrets. Copy this file to `.env` and adjust values for your environment.

# PUBLIC_INTERFACE
# Base URL of the backend API (no trailing slash). Example for local dev:
REACT_APP_BACKEND_BASE_URL=http://localhost:8000
